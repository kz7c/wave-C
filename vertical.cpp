#include <bits/stdc++.h>
#include <thread>
#include <chrono>
using namespace std;

int main(){
    while (true){
        cout << "#      " << endl;
        this_thread::sleep_for(chrono::milliseconds(20));
        cout << " #     " << endl;
        this_thread::sleep_for(chrono::milliseconds(20));
        cout << "  #    " << endl;
        this_thread::sleep_for(chrono::milliseconds(20));
        cout << "   #   " << endl;
        this_thread::sleep_for(chrono::milliseconds(20));
        cout << "    #  " << endl;
        this_thread::sleep_for(chrono::milliseconds(20));
        cout << "     # " << endl;
        this_thread::sleep_for(chrono::milliseconds(20));
        cout << "      #" << endl;
        this_thread::sleep_for(chrono::milliseconds(20));
        cout << "     # " << endl;
        this_thread::sleep_for(chrono::milliseconds(20));
        cout << "    #  " << endl;
        this_thread::sleep_for(chrono::milliseconds(20));
        cout << "   #   " << endl;
        this_thread::sleep_for(chrono::milliseconds(20));
        cout << "  #    " << endl;
        this_thread::sleep_for(chrono::milliseconds(20));
        cout << " #     " << endl;
        this_thread::sleep_for(chrono::milliseconds(20));
        cout << "#      " << endl;
        this_thread::sleep_for(chrono::milliseconds(20));
    }

}